[pytest]
asyncio_mode = auto
# Use a single event loop for all tests (pytest-asyncio 1.x).
# Prevents asyncpg connections in stripe_service / any QueuePool engine from
# being created in one test's loop and then reused (and failing) in the next.
asyncio_default_fixture_loop_scope = session
